>add driver connector jar to project/use maven dependency

setup the database.
Here I am using MySQL database.

docker setup
>docker pull mysql:8.0
>docker run --name mysql-container -e MYSQL_ROOT_PASSWORD=mypassword -p 3306:3306 -d mysql:8.0
>docker exec -it mysql-container mysql -uroot -p

>create database testdb;
>use testdb;
>CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE
);
>Insert into users (name, email) values ('Mehak', 'mminda@opentext.com');
>Insert into users (name, email) values ('Jalaj', 'jalajminda@opentext.com');